const Settings = (theme) => {
  return {
    position: "bottom-center",
    autoClose: 2000,
    hideProgressBar: false,
    closeOnClick: false,
    pauseOnHover: true,
    draggable: false,
    progress: 0,
    theme: theme,
  };
};

export default Settings;
